# Alts round 6 — legacy tag bounce + floor cache — 2026-08-01

## Old paper tags → alts

- Root `vercel.json` redirects `app.…/g/…`, `/garments/…`, `/t/…`, `/e-ticket/…` → alts
- Webapp `/g/` and `/garments/` SPA routes bounce to alts job card
- ERP scanner resolves `app.…/g/…` the same as alts garment paths

## Floor consistency

- Scanner Mark Paid confirms before creating Payment Entry; refreshes pickup/home/shop queries
- Garment status/complete invalidates shop floor + home stats
- Ticket list defaults to **NYC** origin (HOU rows hidden from FOH lists)
- Pay OG preview + POD “view board” use alts URLs
- Soft warn once if `E_TICKET_SECRET` unset

## Tests

- `scanRoutes.test.ts` covers app-host garment QR + alts `/pay/`
