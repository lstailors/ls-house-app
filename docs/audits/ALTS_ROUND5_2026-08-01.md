# Alts round 5 — hardware QR + floor polish — 2026-08-01

## Hang-tag / thermal QR host (HER-55 P2)

Epson / ERP thermal code now **forces** `https://alts.lstailors.com` for receipt and garment-tag QRs when settings still point at `app.lstailors.com`. Deploy the `ls_alterations` thermal module to ERP for printers to pick this up.

## Floor UX

- Garment job card: clear 401 / 404 messages + Sign in that returns to the tag
- Login restores `pathname + search` from RoleGuard redirect
- Thermal paid “receipts” use Sales Invoice (payment receipt); master QR matches pay/e-ticket
- Ticket detail Transfer → deep-link `/transfers?ticket=…` (at-home / NYC)
- Transfers honors `?ticket=` query
- Scanner toast when auto-opening a garment job card

## API hardening

- `/api/garment/*` — 15s timeout, null → 404/502 with ERP snippet
- `/api/print/receipt` accepts `invoice` | `ticket_name` | `ticket`
- Print config `app_base_url` falls back to alts

## Still needs human hands

- Physical Epson / LabelLife dry-run (HER-55 checklist)
- Confirm ERP deploy of thermal `api.py` (or update LSH Print Settings `app_base_url` manually)
- Reload Alteration Ticket Line DocType for photo fields (round 3)
