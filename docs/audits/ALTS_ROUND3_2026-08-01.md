# Alts audit round 3 — 2026-08-01

## P2-7 Line photo mapping

- Intake create sends `client_line_key` (cart line id) + `estimated_minutes` on each ERP line
- Photo upload matches **client_line_key → lineIdx within garment → last line on garment**
- File names encode `G1-L0-<key8>-…` so GET `/photos` can return `lineIdx` / `lineKey`
- DocType `Alteration Ticket Line` gains `line_photos`, `estimated_minutes`, `client_line_key` (deploy ERP app to activate field writes)

## P2-9 Edit ticket metadata

- `PATCH /api/alterations/:id/full` preserves preset, notes, minutes, status, tailor, photos when recreating child rows
- Billable tickets re-run `prepare_alteration_invoice` after edit
- Edit Ticket drawer sends notes / minutes / preset / ERP row name

## UX / shell leftovers

- **PWA** — removed static `manifest-alts.json` link; VitePWA injects `manifest.webmanifest` only
- **`/g` + legacy `/garments`** — wrapped in RoleGuard (login redirect instead of blank 401)
- **`/customers/new`** — real create form (name/phone/email/address) → ERP customer → profile
- **Intake** — warranty / on-order also require work lines on every alter garment (API + client)

## Ops note

After merge, migrate/reload the Alteration Ticket Line DocType on ERP so `line_photos` / `client_line_key` / `estimated_minutes` exist. Until then, File attachments + filename encoding still associate photos; line_photos updates no-op if the field is missing.
