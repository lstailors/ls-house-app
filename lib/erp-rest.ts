// Server-side ERPNext REST client export for Next-style routes in this repo.
// The implementation lives with the Hono backend so all deployments share one
// auth/config/error-handling path.
export * from "../backend/src/lib/erp-rest";
